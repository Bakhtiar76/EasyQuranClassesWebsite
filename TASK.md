# TASK — Configure Claude Code + Local WordPress Development for Easy Quran Classes

## Purpose

Configure this repository and local machine so Claude Code can build, design, test and package the Easy Quran Classes WordPress/Elementor website **locally** and later hand off a safe release for manual cPanel deployment.

### Confirmed hosting constraint

The client's cPanel account has **no Shell/SSH/Terminal access**.

Therefore:
- do not attempt remote SSH;
- do not plan remote WP-CLI;
- do not configure rsync/SCP deployment;
- do not install cPanel/SSH/database MCP servers;
- do not build the website directly in production;
- cPanel interaction is deployment-stage, user-controlled web UI work.

This task is for environment/tooling setup only. Do not start the website pages until setup is verified.

---

# 1. Read first

Read completely:
1. `CLAUDE.md`
2. `DESIGN.md`
3. `CPANEL-WORKFLOW.md`
4. `.gitignore`
5. all `.claude/rules/*.md`
6. all `.claude/skills/*/SKILL.md`
7. this `TASK.md`

If instructions conflict, use the safer rule and report the conflict.

---

# 2. Mandatory implementation rules

Before any implementation create this TODO and keep it updated:

- [x] Scan repository and relevant local WordPress/project state.
- [x] Identify reusable code, styles, helpers, libraries, hooks, templates, skills or config. (all 6 rules, both hooks, 14 skills, `.gitignore` reused unchanged)
- [x] If reuse is not possible, state briefly why a new implementation is necessary. (no local stack existed — see Phase B)
- [x] Define the smallest correct implementation.
- [x] Check native WordPress/Elementor/existing dependencies before custom code.
- [x] Identify the correct existing file/location before creating anything.
- [x] Define realistic edge cases and verification first.
- [x] Implement only the minimal required change.
- [x] Test every modified code path and relevant edge cases.
- [x] Remove dead/obsolete/duplicated/debug code exposed by the change when safely in scope. (stale `EQC_SSH_*` block removed from `.env.example`; test-only SQL exports and staged plugin zip cleaned up after verification)
- [x] Update relevant docs/checklists.
- [x] Keep `CLAUDE.md` current with durable knowledge only and under 1000 lines. (238 lines)
- [x] Review Git status/diff and ensure no secrets, generated release files or unrelated changes are included.
- [x] Run applicable verification and report manual checks.

Never create empty, duplicate or unnecessary files/folders.

---

# 3. Git / GitHub workflow

Inspect existing Git workflow first and preserve safe existing conventions.

If no workflow exists:
- `main` = production-ready code/config;
- use focused `feature/*`, `fix/*`, `chore/*` branches when appropriate;
- do not force-push or rewrite shared history;
- do not push unless explicitly authorized.

Before every commit:

```bash
git status
git diff --check
git diff
```

Then stage only intended files and review the staged diff.

Commit messages must be clean, clear, concise and brief, normally one line, e.g.:

```text
chore: configure local wordpress workflow
feat: add homepage hero styles
fix: correct mobile navigation spacing
```

Do not use long AI-style messages. Never add `Generated by Claude`, `Generated with AI`, `Co-Authored-By: Claude`, or similar attribution.

---

# 4. Phase A — audit only

Do not install or modify anything yet.

## Repository audit
Inspect:
- repository root;
- current branch/status/remotes;
- existing WordPress files;
- current `.claude/` configuration;
- existing rules/skills/hooks/settings;
- `CLAUDE.md`, `DESIGN.md`, `CPANEL-WORKFLOW.md`;
- package/config files;
- current theme/plugin directories;
- tests/lint/build tools;
- docs.

## Local toolchain audit
Report versions/availability of:
- Claude Code
- Git
- GitHub CLI if present
- Docker Desktop / Docker Engine
- Docker Compose
- Node.js
- npm
- PHP
- Composer
- WP-CLI
- Chrome/Chromium
- Playwright CLI
- Lighthouse
- LocalWP if detectable without invasive scanning

Identify whether Claude runs in native Windows, WSL, Linux or macOS.

## Security audit
Do not read secret values.

Check for:
- repository inside an unsuitable broad/cloud-sync directory;
- tracked `.env`, `wp-config.php`, DB dumps, backups or keys;
- overly broad Claude permissions/MCPs;
- local browser auth state inside repo;
- production credentials mixed into project files.

Report findings before setup changes.

---

# 5. Phase B — choose one local WordPress stack

Use the existing healthy stack if one already exists.

If none exists, choose the simplest reliable option in this order:

1. **Docker Desktop + Docker Compose** when available/healthy and compatible with the user's environment. Prefer this when Claude needs a reproducible WordPress + database + WP-CLI stack it can operate directly.
2. **LocalWP** if Docker is unavailable/unreliable or the user prefers a GUI-managed stack.
3. **XAMPP/WAMP** only if the first two are impractical.

Do not install two parallel stacks.

Before installing a system-wide tool, report:
- why it is needed;
- what will be installed;
- whether admin privileges are needed;
- rollback/uninstall path;
- any port conflicts;
then request approval when required.

Once cPanel PHP version is known, align the local PHP version as closely as practical.

---

# 6. Phase C — configure local WordPress

Create/configure the local site only after the stack is approved.

Requirements:
- local-only URL with no production write target;
- WordPress installed and reachable;
- Elementor Free installed locally;
- Hello Elementor installed locally;
- child theme created only if needed by architecture;
- WP debug configured for local use without exposing secrets;
- pretty permalinks enabled;
- search-engine indexing discouraged locally;
- admin credentials kept outside Git;
- local mail sending disabled/captured unless explicitly needed;
- uploads and DB treated as mutable local state, not Git source.

Do not install extra plugins yet except those required for the approved local stack or explicitly approved foundational functionality.

If using Docker, prefer project-scoped Compose configuration and persistent local volumes. Do not commit DB data or WordPress secrets.

---

# 7. Phase D — normalize Claude rules

Reuse/update existing files. Required areas:

```text
.claude/rules/security.md
.claude/rules/wordpress.md
.claude/rules/design.md
.claude/rules/deployment.md
.claude/rules/implementation.md
.claude/rules/git.md
```

Rules must reflect:
- local-first WordPress development;
- no remote shell;
- cPanel manual deployment boundary;
- reuse-first/minimal-code/testing rules;
- concise Git commits with no AI attribution.

Do not duplicate long instructions across files.

---

# 8. Phase E — permissions and hooks

Inspect the installed Claude Code version/current schema before writing `.claude/settings.json`.

Keep machine-specific permissions in ignored local settings where appropriate.

## Permission goals
Deny/read-protect obvious secrets and backup/release artifacts where supported.

Require confirmation for high-impact local actions such as:
- `git push`;
- force push/history rewrite;
- broad file deletion;
- local DB reset/drop/import;
- broad WordPress search-replace that mutates the working DB;
- system-wide package installs;
- commands targeting the production domain;
- release packaging/deployment actions that could contain secrets.

Do **not** create SSH/cPanel allow rules because remote shell is unavailable.

## Hooks
Create only useful local hooks:

### Destructive command guard
Protect against broad delete, destructive Git commands, local DB drop/reset and accidental commands targeting production.

### Lightweight post-edit validation
Use cheap targeted checks only, e.g. PHP syntax or JSON parse when applicable.

### Optional completion guard
Only if reliable and low-noise; check incomplete TODOs, unverified changes and temporary/debug artifacts.

Test each hook with harmless synthetic examples.

---

# 9. Phase F — project skills

Reuse/update existing skills. Required project skills:

```text
wp-audit
wp-cli-safe
elementor-build
visual-qa
seo-review
performance-audit
cpanel-audit
backup-verify
release-check
implementation-workflow
git-checkpoint
wordpress-debug
plugin-evaluation
```

Important reinterpretation:
- `wp-audit` = local WordPress audit;
- `wp-cli-safe` = local WP-CLI only;
- `cpanel-audit` = manual/read-only checklist based on user-provided cPanel values/screenshots, not shell access;
- `release-check` = create/validate a cPanel-ready release package; it does not deploy automatically.

---

# 10. Phase G — efficient local tooling

## WP-CLI
Required locally when supported by the chosen stack.

Verify read commands and database export capability. Do not expose DB secrets.

## Playwright CLI
Recommended for local WordPress/Elementor/browser QA when Node.js 20+ is available.

Use the official documented installation method. Verify against localhost/a harmless page.

Do not store WordPress admin auth state in Git.

## Lighthouse
Use on demand during performance phase; do not create a large Node project solely for it.

## PHP language intelligence
If meaningful custom PHP exists, evaluate the official Claude PHP LSP plugin. Skip if custom PHP remains trivial.

## GitHub tools
Only if the repo is actually hosted on GitHub and PR/issue operations are useful. Not required for local commits.

---

# 11. Phase H — MCP and plugin policy

Initial required MCP set: **none**.

> **Superseded (2026-09-07).** Three local-only MCP servers are now configured for Claude Code
> (`novamira-localhost`) and Codex (`novamira-localhost`, `chrome-devtools`, `context7`).
> See `TOOL-INVENTORY.md`, `README-SETUP.md` §4–§5, and §19 below. The bans in the list that
> follows still stand.

Do not install:
- cPanel MCP;
- SSH MCP;
- unrestricted filesystem MCP;
- direct database MCP;
- browser-session/credential MCP.

These add risk and do not solve the confirmed no-shell hosting workflow.

A WordPress MCP may only be evaluated later for **local development** if it provides a concrete benefit over WP-CLI/browser automation, is official/trusted, and is approved first. Never install it on production as part of this setup.

Plugins: prefer Anthropic official marketplace only, and only when materially useful.

---

# 12. Phase I — local browser/Elementor workflow

Set up a repeatable local workflow:

1. start local WordPress;
2. confirm homepage and `/wp-admin/` load;
3. use local WordPress Admin/Elementor;
4. use Playwright for repeatable browser checks where reliable;
5. visually verify screenshots at desktop/tablet/mobile sizes;
6. keep Elementor page data in the local DB, not direct JSON/SQL edits;
7. make local DB/filesystem backup checkpoints at major milestones.

If browser automation for a particular Elementor action is unreliable, stop and give the user a concise manual action rather than forcing brittle automation.

---

# 13. Phase J — cPanel release workflow setup

Read `CPANEL-WORKFLOW.md`.

Do not connect to cPanel during setup unless the user explicitly asks for a read-only browser review.

Prepare the local release process to support:
- local filesystem backup;
- local DB backup;
- production domain confirmation;
- serialization-safe production URL SQL export;
- reviewed ZIP/archive creation;
- release manifest/checklist;
- manual cPanel File Manager upload/extract;
- manual DB creation/import through phpMyAdmin;
- post-deploy Elementor/cache/permalink checks;
- rollback.

The production SQL transformation must use serialization-safe WordPress tooling, not broad raw SQL replacement.

---

# 14. Test the setup

Verify:
- `CLAUDE.md`/rules/skills are discoverable;
- settings parse;
- hooks block/ask correctly;
- Git ignore catches DB dumps, release archives, auth state and secrets;
- local WordPress starts/stops reliably;
- local `/wp-admin/` works;
- local WP-CLI can inspect the site;
- Playwright works against local site if installed;
- no remote shell/cPanel tooling was configured;
- no production credentials were stored.

Do not create destructive tests.

---

# 15. Documentation and tool inventory

Maintain one concise inventory containing:

| Tool / Skill / Plugin / MCP | Version/source | Scope | Purpose | Access | Risk | Status |
|---|---|---|---|---|---|---|

Include local stack, WP-CLI, Playwright, hooks, skills and optional plugins.

For cPanel record only capabilities/state needed for deployment — never passwords or secret values.

---

# 16. Final Git checkpoint

Before setup completion:
1. `git status`;
2. `git diff --check`;
3. review full diff;
4. remove temporary setup artifacts;
5. verify `.gitignore`;
6. verify no DB dumps, release files, auth state or credentials;
7. update checklists/docs/`CLAUDE.md`;
8. propose a concise commit message.

Preferred example:

`chore: configure local wordpress workflow`

Do not push unless explicitly authorized.

---

# 17. Completion criteria

- [x] repository/toolchain audit complete;
- [x] one primary local WordPress stack selected (Docker Desktop);
- [x] local WordPress runs successfully (WP 7.1 at `http://localhost`, HTTP 200, pretty permalinks verified end-to-end) — port mapping is `80:80`; an earlier `8080:80` mapping broke WordPress self-loopback (`cURL error 7`) and was corrected, see §19;
- [x] Elementor Free/Hello available locally (Elementor 4.2.4 active, Hello Elementor 3.5.1 parent, child theme active);
- [x] local WP-CLI strategy works (containerized `wpcli` service, verified via install/config/export/import);
- [x] project rules/skills reflect local-first/no-shell deployment;
- [x] permissions/hooks validated (both hooks tested with synthetic input; settings.json is valid JSON);
- [x] browser QA tooling configured when justified (Playwright project-local + `chrome-devtools` MCP, both verified against the live site);
- [x] no unnecessary MCPs/plugins installed — Novamira is a deliberate, user-approved local-only exception, installed manually via WP Admin (Bash-based install stayed blocked by the auto-mode classifier) and connected via MCP (`.mcp.json`, verified live). WPVibe was installed by the user directly, outside this plan; deactivated since it cannot reach `localhost`.
- [x] no cPanel/SSH/database MCP installed;
- [x] cPanel release workflow documented (domain/404 status recorded in `CPANEL-WORKFLOW.md`; search-replace export dry-run and real export both verified without mutating the local DB);
- [x] DB/backup/release artifacts ignored by Git (verified with `git check-ignore` on `local/.env`, `local/backups/*`, `tests/visual/node_modules`, `tests/visual/test-results`);
- [x] docs/checklists updated;
- [x] Git diff reviewed;
- [x] no production changes occurred.

---

# 18. Final report format

## Setup Summary

## Environment
- OS/shell
- project root
- local WordPress stack
- local URL
- Claude Code version
- Git branch/remotes

## Reused Existing Setup

## Files Changed

## Local WordPress
- stack
- WordPress/PHP/DB versions
- Elementor/Hello state
- WP-CLI status

## Skills / Rules / Hooks

## Tools / Plugins / MCP

## Verification

## cPanel Release Readiness
State what still must be manually confirmed in cPanel.

## Risks / Open Questions

## Suggested Git Commit
One short natural commit message, no AI attribution.

---

# 19. Setup Outcomes — Issues Encountered & Fixes

Recorded after the fact. This is the historical log; the **reproducible procedure and the full
symptom → cause → fix detail live in `README-SETUP.md` §7**. Both cross-reference.

| Area | Issue | Resolution | Detail |
|---|---|---|---|
| Docker | `8080:80` port mapping broke WordPress self-loopback, Novamira REST self-check (`cURL error 7`), WP-Cron | Mapping changed to `80:80`; host port must equal Apache's internal port | README-SETUP §7, CLAUDE.md "Local Environment" |
| Docker | Containerized `wp-cli` (Alpine `www-data`=82) couldn't write to the volume owned by the Apache image (Debian `www-data`=33) | `wpcli` service pinned to `user: "33:33"` | README-SETUP §7 |
| WordPress | App Passwords / OAuth refused over plain HTTP | `WP_ENVIRONMENT_TYPE=local` via `WORDPRESS_CONFIG_EXTRA` | README-SETUP §7 |
| Git Bash | `/`-leading args rewritten to Windows paths by MSYS | Prefix commands with `MSYS_NO_PATHCONV=1` | README-SETUP §7 |
| Novamira MCP | `.mcp.json` `${NOVAMIRA_APP_PASSWORD}` resolves from OS env, not `local/.env` | Set as Windows **User** env var; restart Claude Code | README-SETUP §2.3, §7 |
| Novamira CLI | Windows Credential Manager backend fails (not sandbox-specific) | `NOVAMIRA_CREDENTIAL_BACKEND=file` (User env var) | README-SETUP §7 |
| Novamira CLI | File backend's "exactly one ACL entry" check failed (inherited `CodexSandboxUsers` ACE) | `icacls <dir> /inheritance:r /grant:r "<user>:(OI)(CI)F"` on `%LOCALAPPDATA%\Novamira\{Credentials,Cache}` | README-SETUP §7 |
| Novamira CLI | `Too many registrations` (OAuth client cap transient) | Delete `_transient_novamira_oauth_dcr_0_<hash>` pair | README-SETUP §7 |
| Claude Code | Auto-mode classifier intermittently denied "novamira" commands and the plugin install | Retry / reword; Novamira installed manually via wp-admin | Completion criteria §17 |
| Codex | Docker unreachable from both Codex sandbox modes | Docker/WP-CLI commands approved out of sandbox; documented in `AGENTS.md` | README-SETUP §5.4, §7 |
| Codex | `npx.ps1` blocked by PowerShell execution policy; Codex can't spawn `.cmd` with working stdio | MCP servers registered as `cmd /c npx -y <pkg>` by `tools/codex/setup-codex.ps1` | README-SETUP §7 |
| Codex | Strips `*PASSWORD*` env vars before spawning MCP servers | `tools/codex/novamira-mcp.cmd` reads the password from `local/.env` | README-SETUP §5.3, §7 |
| Codex | MCP tools deferred behind tool-search — look "missing" | Expected; the model must search its tools. Not a bug. | README-SETUP §7 |
| Tooling | WPVibe can't reach `localhost`; `greptile` MCP 403 | WPVibe deactivated; `greptile` disabled | TOOL-INVENTORY.md |

## Codex parity (added 2026-09-07)

Codex now has the same working environment as Claude Code on this repo:

- `AGENTS.md` (repo root) — thin pointer to `CLAUDE.md` + `.claude/rules/*.md` + Codex-specific notes.
- `tools/codex/setup-codex.ps1` — idempotent, with `-Verify`. Registers `novamira-localhost`,
  `chrome-devtools`, `context7`; syncs the 14 project skills to `~/.codex/skills/eqc-*`; trusts the repo.
- `tools/codex/novamira-mcp.cmd` — credential-safe Novamira launcher (no secret in `~/.codex/config.toml`).
- Verified end-to-end: Codex called `mcp-adapter-discover-abilities` (36 abilities) and
  `context7/resolve-library-id`.
